https://leetcode.com/problems/validate-binary-search-tree/description/

Question:
root: [2,1,3]
output true

root:[5,1,4,null,null,3,6]
output false

-------------------------------------------------------------------------------------------------------------------------------------------
Recursive approach using MIN and MAX value

Solution:
The min value will always be the one to the left
The max value will always be the one to the right

Solution:
/**
 * Definition for a binary tree node.
 * public class TreeNode {
 *     int val;
 *     TreeNode left;
 *     TreeNode right;
 *     TreeNode() {}
 *     TreeNode(int val) { this.val = val; }
 *     TreeNode(int val, TreeNode left, TreeNode right) {
 *         this.val = val;
 *         this.left = left;
 *         this.right = right;
 *     }
 * }
 */
class Solution {
    public boolean isValidBST(TreeNode root) {
        return validate(root,null,null);
    }
    public boolean validate(TreeNode root,Integer min,Integer max){
        if(root==null)return true;
        if(min!=null && root.val<=min)return false;
        if(max!=null && root.val>=max)return false;
        return validate(root.left,min,root.val) && validate(root.right,root.val,max);
    }
}



--------------------------------------------------------------------------------------------------------------------------------------------
NAIVE APPROACH:
A basic way is that I make use of the way INORDER gives ascending order values

/**
 * Definition for a binary tree node.
 * public class TreeNode {
 *     int val;
 *     TreeNode left;
 *     TreeNode right;
 *     TreeNode() {}
 *     TreeNode(int val) { this.val = val; }
 *     TreeNode(int val, TreeNode left, TreeNode right) {
 *         this.val = val;
 *         this.left = left;
 *         this.right = right;
 *     }
 * }
 */
class Solution {
    ArrayList<Integer>ar=new ArrayList<>();
    public boolean isValidBST(TreeNode root) {
        validate(root);
        for(int i=1;i<ar.size();i++){
            if(ar.get(i-1)>=ar.get(i))return false;
        }
        return true;
    }
    public void validate(TreeNode root){
        if(root==null)return;
        validate(root.left);
        ar.add(root.val);
        validate(root.right);
    }
}
