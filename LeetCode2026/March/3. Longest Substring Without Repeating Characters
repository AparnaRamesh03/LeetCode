https://leetcode.com/problems/longest-substring-without-repeating-characters/description/

Question:
Example 1:

Input: s = "abcabcbb"
Output: 3
Explanation: The answer is "abc", with the length of 3. Note that "bca" and "cab" are also correct answers.
Example 2:

Input: s = "bbbbb"
Output: 1
Explanation: The answer is "b", with the length of 1.
Example 3:

Input: s = "pwwkew"
Output: 3
Explanation: The answer is "wke", with the length of 3.
Notice that the answer must be a substring, "pwke" is a subsequence and not a substring.

SOLUTION:
----------------------------------------------------------------------------------------------------------------------------------------------------------------------------
Brute force will be going through each loop the O(n2) approach
----------------------------------------------------------------------------------------------------------------------------------------------------------------------------
Optimal:
TIME COMPLEXITY: O(n)
SPACE COMPLEXITY: O(n)

abcabcab
z b x y b c z a      map z 1
s
f

z b x y b c z a     map z 1  max=(0,0-0+1)=1
s f                     b 2  max=(1,1-0+1)=2
s   f                   x 3  max=(2,2-0+1)=3
s     f                 y 4  max=(3,3-0+1)=4
slow=max(0,2)
    s   f               b 5  max=(4,5-2+1)=4
          f             c 6  max=(4,6-2+1)=5
    s      f            
slow=max(2,1)=2         
    s        f

max=5
CODE:
class Solution {
    public int lengthOfLongestSubstring(String s) {
        HashMap<Character,Integer>map=new HashMap<>();
        int slow=0,fast=0,max=0;
        while(fast<s.length()){
            if(map.containsKey(s.charAt(fast))){
                slow=Math.max(slow,map.get(s.charAt(fast)));
            }
            map.put(s.charAt(fast),fast+1);
            max=Math.max(max,fast-slow+1);
            fast++;
        }
        return max;
    }
}
