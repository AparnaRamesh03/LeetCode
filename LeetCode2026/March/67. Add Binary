https://leetcode.com/problems/add-binary/description/


Question:
Example 1:

Input: a = "11", b = "1"
Output: "100"
Example 2:

Input: a = "1010", b = "1011"
Output: "10101"


Solution:
use a carry on which will carry the divident value
and remainder value is just appended to the stringbuilder

class Solution {
    public String addBinary(String a, String b) {
        int i=a.length()-1;
        int j=b.length()-1;
        int c=0;
        StringBuilder sb=new StringBuilder();
        while(i>=0 || j>=0){
            int res1=0,res2=0;
            if(i>=0){
                res1=a.charAt(i)-'0';
            }
            if(j>=0){
                res2=b.charAt(j)-'0';
            }
            int val=res1+res2+c;
            sb.append(val%2);
            c=val/2;
            i--;j--;
        }
        if(c==1)sb.append(c);
        return sb.reverse().toString();

    }
}
